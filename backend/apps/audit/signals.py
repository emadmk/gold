"""Signal handlers that produce audit events for side-effect free models."""
from __future__ import annotations

# Currently no signals — kept as an import target so AuditConfig.ready
# can register them in the future without changing apps.py.
